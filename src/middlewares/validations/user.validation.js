/**
 * Get User Validator
 * @param {object} req
 */
// eslint-disable-next-line consistent-return
const getUserValidator = async (req, res, next) => {
  next();
};

export default {
  getUserValidator,
};
